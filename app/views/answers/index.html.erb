<!-- todo think about it-->
<h1>You are <%= params[:a] == 'yes' ? 'Y' : 'N' %> person (change this)</h1>
<h2>Please answer the questions listed below</h2>


<%= form_tag({:controller => "answers", :action => "create"}, :method => "post") do %>

    <div class="field">
      <%= label_tag(:q1, @questions[0]) %><br />
      <%= text_area_tag(:q1)%>
    </div>
    <div class="field">
      <%= label_tag(:q2, @questions[1]) %><br />
      <%= text_area_tag(:q2)%>
    </div>
    <div class="field">
      <%= label_tag(:q2, @questions[2]) %><br />
      <%= text_area_tag(:q3)%>
    </div>
    <div class="actions">
      <%= submit_tag("Save") %>
    </div>
    <%= hidden_field_tag(:a, params[:a]) %>
<% end %>